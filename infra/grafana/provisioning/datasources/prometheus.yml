apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: softsentry-prometheus
    access: proxy
    # resolve บน internal compose network (service name: prometheus)
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
