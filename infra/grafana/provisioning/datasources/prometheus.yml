apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: softsentry-prometheus
    access: proxy
    # Resolves on the internal compose network (service name: prometheus).
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
